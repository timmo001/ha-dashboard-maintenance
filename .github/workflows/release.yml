---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@8eac938d57bbcff242ec16ffbb3bbcea0d45b065 # master
    with:
      dist-glob: "dist/ha-dashboard-maintenance.js"
    permissions:
      contents: write
