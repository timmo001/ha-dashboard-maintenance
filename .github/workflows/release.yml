---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@4a140067c8acd609167bffb17c01e63020d06d41 # master
    with:
      dist-glob: "dist/ha-dashboard-maintenance.js"
    permissions:
      contents: write
