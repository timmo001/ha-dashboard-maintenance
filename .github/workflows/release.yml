---
name: Release

# yamllint disable-line rule:truthy
on:
  release:
    types:
      - published
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: timmo001/workflows/.github/workflows/ha-card-release.yml@f3f791040a47a4438299d421084d98f1affe83bb # master
    with:
      dist-glob: "dist/ha-dashboard-maintenance.js"
    permissions:
      contents: write
